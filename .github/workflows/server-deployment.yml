name: "Server Deployment" 
on:
  push:
    branches: [main] 
jobs:
  deployment:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Say Hello!
        run: echo "Hello Suresh!"
        
      - name: Deploy code to Server
        env:
         SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          echo $SSH_KEY | base64 -d > ssh-key   
          chmod 400 ssh-key    
             ssh -i ssh-key -o StrictHostKeyChecking=no ec2-user@52.87.198.40 "/var/www/html/git-ec2 && GIT_SSH_COMMAND='ssh -i /home/ec2-user/.ssh/authorized_keys' git pull origin main"
